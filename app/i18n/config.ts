/**
 * Lightweight i18n constants – no locale JSON imports.
 * Safe to import from middleware / edge runtime.
 */
export const cookieName = "tekis-lang"
export const languages = ["en", "fi"]
export const fallbackLang = "fi"
